<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--<item name="android:dialogTheme">@style/PoshtarDialog</item>-->
        <!--<item name="android:alertDialogTheme">@style/PoshtarDialog</item>-->
    </style>

    <style name="PoshtarDialog" parent="@style/Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowTitleStyle">@style/DialogTitle</item>
    </style>

    <style name="DialogTitle" parent="Base.DialogWindowTitle.AppCompat">
        <item name="android:gravity">center</item>
    </style>

</resources>
